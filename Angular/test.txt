v1.2及以下 不多
v1.3及以上 主流
v2.x  typescript写的 与1.3多完全不同
v4.x 最近出的
学习v1.64 
MVP p-->presenter主持人  MV没有直接的关系 MV没有耦合
MV VM （一部分简单的逻辑直接放到html里面，而且一目了然） 取消臃肿 直接 粗暴
Augular.js MVVM
两大特色：
1.双向绑定 ng-models  数据变了<-->视图变化
*自动同步数据和视图
2.ng-bind 输出 单向
ng-app 先理解为是一个范围

angular表达式
{{}}  可以出现在任何你想出现的位置